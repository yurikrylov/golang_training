package utils

import (
	"fmt"
	"strings"
)

func StringTasks1() bool {
	var str, l string
	fmt.Scanf("%s %s", &str, &l)
	result := strings.Contains(str, l)

	return result
}

func StringTasks2() bool {
	var str, l string
	fmt.Scanf("%s %s", &str, &l)
	result := strings.Contains(str, l)

	return result
}
